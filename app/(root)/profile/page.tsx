//Actions
import { getCurrentUser } from "@/actions/fetch/currentUser";

const page = async () => {

    const currentAdmin = getCurrentUser();

    return ( 
        <main>
            
        </main>
     );
}
 
export default page;